%h1 
  New task for user
  = @owner_user.email 
.row
  .col-md-6.col-md-offset-3
    = simple_form_for(@task, url: user_tasks_path) do |f|
      = f.input  :name
      = f.input  :description, as: :text
      = f.button :submit, 'Create new task', class: "btn btn-primary"